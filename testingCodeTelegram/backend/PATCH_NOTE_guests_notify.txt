# File: backend/app/routers/guests_notify_patch.py (apply into existing guests.py after create_guest commit)
# Pseudocode patch (for your reference if applying manually):
#
# from fastapi import BackgroundTasks
# from ..utils.notifications import send_telegram_photos
#
# @router.post("/", response_model=schemas.GuestRead, dependencies=[Depends(require_roles("admin", "manager", "staff"))])
# def create_guest(..., bg: BackgroundTasks):
#     ... db.add(guest); db.commit(); db.refresh(guest)
#     # Load images (none yet on create), still send text
#     caption = f"Khách mới: {guest.full_name}\nCCCD: {guest.id_card_number}\nNhà cung cấp: {guest.supplier_name}\nBiển số: {guest.license_plate}\nĐăng ký bởi: {user.full_name}"
#     bg.add_task(send_telegram_photos, caption, [])
#     return guest
